apiVersion: v1
kind: Service
metadata:
  name: {{ include "deejai.fullname" . }}
  labels:
    {{- include "deejai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "deejai.selectorLabels" . | nindent 4 }}
